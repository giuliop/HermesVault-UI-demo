Currently using a static, full bundle for pera-connect. Use webpack to optimize js bundle before release

Don't use CDNs for js/css in production

Prevent submit buttons to fire again before receiving response

what happens with errors? e.g., with http.StatusInternalServerError ?
